[project]
name = "fast-api-demo"
version = "0.1.0"
description = "Demonstrate use of FastAPI from basics to advanced."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.53.0",
]
